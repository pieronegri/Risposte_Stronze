<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="pieronegri.rispostestronze.ui.controller.MainActivity"
    >
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/container"
        android:background="@color/colorBackground"
        >
        <!-- Main content -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_navigation"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:background="@color/colorText"
            app:itemIconTint="@color/colorBackground"
            app:itemTextColor="@color/colorBackground"
            app:menu="@menu/bottom_nav_menu"
            />

        <ImageButton
            android:id="@+id/Btn_Exit"
            android:layout_width="@dimen/button_width_medium"
            android:layout_height="@dimen/button_height_medium"
            android:background="@color/colorBackground"
            android:layout_gravity="center"
            android:contentDescription="@string/DescriptionBtnExit"
            app:srcCompat="@drawable/esci" />
    </FrameLayout>
</androidx.constraintlayout.widget.ConstraintLayout>